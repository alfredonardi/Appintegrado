# =============================================================================
# Feature Flags - Controle de Features por Variavel de Ambiente
# =============================================================================
# Copie este arquivo para .env e customize conforme necessario
# Formato: VITE_FEATURE_<NOME_FLAG>=true/false
#
# Exemplos:
# VITE_FEATURE_CASESMODULE=true
# VITE_FEATURE_CLIENTSMODULE=false
# VITE_FEATURE_REPORTSMODULE=false

# Core Features (recomenda-se manter ativado)
VITE_FEATURE_AUTH=true

# Modulos Principais
VITE_FEATURE_DASHBOARD=true
VITE_FEATURE_CASESMODULE=true

# Modulos Opcionais (desativados por padrao para desenvolvimento passo-a-passo)
VITE_FEATURE_CLIENTSMODULE=false
VITE_FEATURE_REPORTSMODULE=false
VITE_FEATURE_SETTINGSMODULE=true
VITE_FEATURE_ANALYTICSMODULE=false

# =============================================================================
# Ambiente de Desenvolvimento
# =============================================================================
# Estes sao setados automaticamente por Vite
# VITE_PORT=5173
# VITE_HOST=localhost

# =============================================================================
# Nhost Configuration
# =============================================================================
# Explicit URLs (preferred)
# VITE_NHOST_AUTH_URL=https://<subdomain>.auth.<region>.nhost.run/v1
# VITE_NHOST_GRAPHQL_URL=https://<subdomain>.graphql.<region>.nhost.run/v1
# VITE_NHOST_STORAGE_URL=https://<subdomain>.storage.<region>.nhost.run/v1
# VITE_NHOST_FUNCTIONS_URL=https://<subdomain>.functions.<region>.nhost.run/v1

# Or set subdomain + region to auto-build the URLs above
# VITE_NHOST_SUBDOMAIN=your-project-subdomain
# VITE_NHOST_REGION=sa-east-1